module.exports = {
  token: "BOT_TOKEN_HERE",
  imgurclientID: "IMGUR_CLIENT_ID",
  giphyapikey: "GIPHY_API_KEY",
  WHid: "WEBHOOK_ID",
  WHtoken: "WEBHOOK_TOKEN",
};

/*
To create a webhook:
head over to the channel's settings where you would like the webhook to be created
head over to integrations, then click on webhooks and create a webhook
copy the url and paste the ID and token in the respective fields
webhook URLs are typically of the stucture https://discordapp.com/api/webhooks/<ID>/<TOKEN>
*/
